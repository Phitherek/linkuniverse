.collection
    %h2= link_to c.name, collection_path(c.link_handle), class: :name
    .description= simple_format c.description
    = render partial: 'collections/collection_metadata', locals: {c: c}
