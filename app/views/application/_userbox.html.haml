.userbox
    - if u.nil?
        %p.status You are not logged in!
        %p.links
            = link_to "Login", login_users_path
            or
            = link_to "Register", register_users_path
    - else
        %p.status
            Welcome
            = u.username + "!"
        %p.links
            = link_to "Logout", logout_users_path, method: :delete
